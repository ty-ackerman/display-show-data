const { Router } = require('express')
const router = Router()
const { Episode } = require('../models/Episode')

// Create /GET route for all episodes + one episode (search)

// Create /POST route for new episode

module.exports = router
